apiVersion: v1
kind: Service
metadata:
  name: bookmood-service
spec:
  selector:
    app: bookmood
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30188
  type: NodePort
